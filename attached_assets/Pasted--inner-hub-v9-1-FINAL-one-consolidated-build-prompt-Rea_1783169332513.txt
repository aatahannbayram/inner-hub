# inner.hub v9.1 FINAL · one consolidated build prompt

## Read this first
This replaces all previous instructions. Apply everything below in one pass. Two of these requirements have been requested before and were not delivered; this time each has a **proof step you must run and show**.

## PRIORITY 0 · Server-side rendering (currently failing)
The live site serves an empty HTML shell: crawlers receive only `<head>` metadata and zero body text. This is unacceptable.
1. Detect the stack. If this is a Vite/CRA single-page app, migrate the page to **Next.js App Router** with all section content as Server Components (only the live clock, form, index rail, and preloader are small client leaves). If already Next.js, remove the top-level `'use client'` from the page and restructure.
2. **Proof you must run and paste into your summary:** after building, run
   `curl -s http://localhost:PORT | grep -o "founding members\|What comes next starts here\|The next wave knows each other"`
   and show the three matches. If they do not appear, the task is not done. Do not report completion without this output.

## PRIORITY 1 · Content is v9.1 (below), verbatim
Replace every word on the page with the copy in this document. Do not paraphrase. Punctuation rule site-wide: **no em dashes, no hyphens-as-punctuation**; the separator everywhere is the middle dot `·`.

### Design system (unchanged, do not touch)
Bone `#F4F1EC` paper ground with static grain · Ink `#0A0A0A` · Inner Green `#18FF85` (squares, `NOW`-type accents, selection only) · Stone `#6E6A63`. Lockup recipe site-wide: `font-family:'Fraunces'; font-style:normal; font-weight:100; font-variation-settings:'opsz' 144,'WONK' 1; letter-spacing:-0.015em` (upright, never italic). Geist body (Inter Tight fallback), JetBrains Mono metadata. Motion: single easing `cubic-bezier(0.16,1,0.3,1)`, masked line reveals, hairlines drawing in, preloader ≤900ms once per session, Lenis, full `prefers-reduced-motion` fallbacks. Marks allowed, complete list: header `inner■hub` lockup · footer `inner-logo-v2.png` (~140px) · monumental footer `inner■` wordmark · favicon = green square only · `inner-og.png`. No i-badge, no tile badge.

### Header
Left: `inner■hub` lockup. Right: ONLY `İSTANBUL 41.00°N 28.97°E · 14:32:07 TRT` with a ticking live clock (Europe/Istanbul, tabular-nums; time only on mobile). No straplines.

### Hero (exactly one viewport, 100svh, nothing after it but section 01)
- Eyebrow (mono): `İSTANBUL → GLOBAL · EST. 2022`
- H1 (Fraunces, large): 
  > What comes next starts here.
- Sub (Geist): 
  > inner.hub is a private circle of founders, builders, and investors. People who meet early and support each other first.
- No SCROLL cue, no spacer elements, no empty bands.

### 01 · THE IDEA · annotation `COMING TOGETHER`
> AI is the center of this circle. Around it are the founders, builders, and investors pushing what comes next. inner.hub brings them together. It starts in İstanbul, and it starts early.

Text only. No diagram in this section.

### 02 · THE FIRST THIRTY-FOUR · annotation `FOUNDING SEATS`
Intro: 
> It starts with thirty-four people, chosen one by one:

Rows (mono label left · Geist right):
- `FOUNDERS` · People building startups, in AI and beyond.
- `BUILDERS` · Engineers and researchers doing serious AI work.
- `INVESTORS` · Angel investors and people from venture funds.

Line, set apart: 
> Some of them are building projects you already know.

Closing: 
> These thirty-four are not just members. They are the founding members of inner.hub.

### 03 · WHAT THIS IS · annotation `THE POINT`
One paragraph, styled as statement text (font-size `clamp(19px, 2.4vw, 26px)`, line-height 1.55, max-width 46ch):
> Big things start here. New ideas are discussed here, tested here, and supported here, by people who can actually build them and fund them.

Nothing else in this section.

### 04 · ENTRY · annotation `BY INVITATION`
- H: 
  > Entry is by invitation. Always.
- Body: 
  > There are no tickets, no tiers, and no public list. Members are put forward from inside the circle, considered with care, and invited personally.
- Steps (mono left · one line right):
  - `YOUR NAME` · Someone inside the circle puts your name forward.
  - `CONSIDERATION` · We take our time. Fit beats fame.
  - `INVITATION` · If it is right, you hear from us directly.

### 05 · THE GATHERING · annotation `SEP 2026 · İSTANBUL` · full-bleed Ink interlude
- H: 
  > The first inner.hub gathering. İstanbul, September 2026.
- Triptych (Fraunces, oversized): **34 / 2 / 1** · mono captions `PEOPLE` · `DAYS` · `CIRCLE`
- Line: 
  > Thirty-four people. Two days. One circle. The first of many.
- **The only diagram on the site:** inline SVG (~320px): thirty-four ~7px squares evenly on an invisible circle (r≈130), thirty-three in Bone at ~85% opacity, one in Inner Green; group rotates ~140s linear infinite; static under reduced motion; mono caption `34 · ONE CIRCLE`; `aria-hidden="true"` plus visually hidden text "Thirty-four squares forming one circle."
- Nothing else: no detail lines, no schedules.

### 06 · WHAT'S NEXT · annotation `IN TIME`
- H: 
  > hub is where it starts.
- Body, statement text (same styling as 03):
  > We are building something bigger, step by step. We announce things when they are real. There is more.

### 07 · REQUEST AN INVITATION · annotation `WE READ EVERYTHING`
- H: 
  > Request an invitation.
- Sub: 
  > Most people arrive by invitation, but good people also find us on their own. Tell us who you are and what you are building.
- Form unchanged: `NAME` · `EMAIL` · `WHO YOU ARE / WHAT YOU'RE BUILDING` · `LINK` (optional) · `WHO INTRODUCED YOU` (optional) · honeypot · `SEND` (Ink fill, Bone text, no green).
- Success (Fraunces): 
  > Received. If it fits, we will be in touch.

### Footer (Ink)
- Mono line: `THE NEXT WAVE KNOWS EACH OTHER · İSTANBUL → GLOBAL`
- **Social icons:** LinkedIn and Instagram, minimal line-style, 20px, Bone at 60% opacity, hover to full Bone (300ms house easing), 20px apart. Desktop: right-aligned on the footer line's row. Mobile: centered beneath it. Placeholders: `href="#"`, `aria-label="inner on LinkedIn"` / `aria-label="inner on Instagram"`. No green, no boxes, no circles.
- `inner-logo-v2.png` (~140px) · `© 2026 inner. İstanbul.` · monumental `inner■` wordmark at the very bottom.

## PRIORITY 2 · Spacing pass
1. Uniform section rhythm: 140px top padding desktop, 90px mobile, every section. Remove min-height rules on content sections and any spacer divs.
2. No vertical gap larger than 220px anywhere between content blocks.
3. Sections 03 and 06 use the statement-text styling above; do not add copy to fill space.

## SEO
- `<title>`: `inner.hub — the inner circle`
- Meta description (update it, currently stale): `A private circle of founders, builders, and investors. İstanbul → global. What comes next starts here.`
- og/twitter descriptions match; `og:image` `/inner-og.png` (1200×630); favicon = green-square set.

## Definition of done · run these and paste results
1. The curl grep from Priority 0 with its three matches.
2. Page-text scan: "—" 0 · " - " 0 · "invitation/invite" only in sections 04 and 07 · "nomination" 0 · "fee/fees/free/price/revenue/sold/paid" 0 · "club" 0 · "dinner/monthly/twice" 0 · "problem/solve/fix" 0 · "room/studio/capital/ring/ecosystem/community" 0 · "AI founders" 0 (the phrase must not appear; "AI" and "founders" exist separately) · "venture funds", "founding members", "projects you already know" present.
3. Seven sections, rail 01–07 tracking, one diagram (in 05), footer icons present, meta description updated, no console errors, no layout shift.

Apply everything, run the proofs, then show me a live preview.